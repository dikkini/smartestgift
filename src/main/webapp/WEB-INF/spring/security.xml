<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
http://www.springframework.org/schema/security
http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <http pattern="/resources/**" security="none" />
	<http auto-config="true" use-expressions="true">
        <intercept-url pattern="/signin" access="isAnonymous()"/>
        <intercept-url pattern="/logout" access="isAuthenticated()"/>
		<intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />

        <form-login login-page="/signin" default-target-url="/" authentication-failure-url="/signin-failure"/>
        <logout logout-url="/logout" success-handler-ref="logoutHandler"/>

		<remember-me user-service-ref="authDAOImpl"/>

        <session-management>
            <concurrency-control max-sessions="1" error-if-maximum-exceeded="true" />
        </session-management>
	</http>

    <!-- Настройки глобальной секурити. Разрешение использовать аннотации. -->
    <global-method-security secured-annotations="enabled">
        <protect-pointcut expression="execution(* com.smartestgift.service.*Service.*(..) )" access="ROLE_USER"/>
    </global-method-security>

    <authentication-manager>
        <authentication-provider user-service-ref="authDAOImpl">
            <password-encoder hash="md5"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>