<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:security="http://www.springframework.org/schema/security"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
          http://www.springframework.org/schema/security
          http://www.springframework.org/schema/security/spring-security-3.2.xsd
          http://www.springframework.org/schema/context
          http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.smartestgift.security"/>

    <security:http pattern="/resources/**" security="none"/>
    <security:http auto-config="false" use-expressions="true">
        <security:intercept-url pattern="/" access="permitAll" requires-channel="http"/>
        <security:intercept-url pattern="/login" access="isAnonymous()" requires-channel="http"/>
        <security:intercept-url pattern="/facebookLogin" access="isAnonymous()" requires-channel="http"/>
        <security:intercept-url pattern="/facebookAuthentication" access="isAnonymous()" requires-channel="http"/>
        <security:intercept-url pattern="/signup" access="isAnonymous()" requires-channel="http"/>
        <security:intercept-url pattern="/signup/social" access="isAnonymous()" requires-channel="http"/>
        <security:intercept-url pattern="/register" access="isAnonymous()" requires-channel="http"/>

        <security:intercept-url pattern="/**" access="isAuthenticated()" requires-channel="http"/>
        <security:intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" requires-channel="http"/>

        <security:form-login login-page="/login"
                             default-target-url="/"
                             authentication-failure-url="/login?error=true"/>

        <security:custom-filter position="LOGOUT_FILTER" ref="logoutFilter"/>

        <security:session-management>
            <security:concurrency-control max-sessions="10" error-if-maximum-exceeded="true"/>
        </security:session-management>

        <security:remember-me services-ref="rememberMeServices" key="127roseQ"/>
    </security:http>

    <!-- Настройки глобальной секурити. Разрешение использовать аннотации. -->
    <!--<security:global-method-security secured-annotations="enabled" pre-post-annotations="enabled">-->
        <!--<security:protect-pointcut expression="execution(* com.smartestgift.service.*Service.*(..) )"-->
                                   <!--access="ROLE_USER"/>-->
    <!--</security:global-method-security>-->

    <security:authentication-manager alias="authenticationManager">
        <security:authentication-provider user-service-ref="userAuthProviderImpl">
            <security:password-encoder ref="encoder"/>
        </security:authentication-provider>
    </security:authentication-manager>

    <bean id="encoder" class="org.springframework.security.crypto.password.StandardPasswordEncoder"/>

    <bean id="rememberMeServices"
          class="org.springframework.security.web.authentication.rememberme.PersistentTokenBasedRememberMeServices">
        <property name="tokenRepository" ref="smartTokenRepository"/>
        <property name="userDetailsService" ref="userAuthProviderImpl"/>
        <property name="key" value="127roseQ"/>
    </bean>

    <bean id="logoutFilter" class="org.springframework.security.web.authentication.logout.LogoutFilter">
        <constructor-arg index="0" value="/login"/>
        <constructor-arg index="1">
            <list>
                <ref bean="rememberMeServices"/>
                <bean id="securityContextLogoutHandler"
                      class="org.springframework.security.web.authentication.logout.SecurityContextLogoutHandler"/>
                <bean id="smartLogoutHandler" class="com.smartestgift.handler.SmartLogoutHandler"/>
            </list>
        </constructor-arg>
        <property name="filterProcessesUrl" value="/logout"/>
    </bean>

</beans>